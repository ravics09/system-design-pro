PORT=3109
REDIS_URL=redis://127.0.0.1:6379
MONGODB_URI=mongodb://127.0.0.1:27017/flashsale
# How long a reservation is held before it expires and returns stock (ms)
RESERVATION_TTL_MS=300000
# Reaper interval (ms) that returns expired reservations to the stock pool
REAP_INTERVAL_MS=2000
