services:
  app:
    build: .
    restart: unless-stopped
    environment:
      PORT: 3120
      FAILURE_THRESHOLD: ${FAILURE_THRESHOLD:-5}
      RESET_TIMEOUT_MS: ${RESET_TIMEOUT_MS:-10000}
    ports:
      - '3120:3120'
