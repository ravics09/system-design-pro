PORT=3132
# A replica-set URI is required for read preferences / lag to take effect, e.g.:
#   mongodb://mongo1:27017,mongo2:27017/replag?replicaSet=rs0
# A standalone URI also works (reads just go to the single node).
MONGODB_URI=mongodb://127.0.0.1:27017/replag
