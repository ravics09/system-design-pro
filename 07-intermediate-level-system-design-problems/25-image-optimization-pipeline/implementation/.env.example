PORT=3125
# Directories for source uploads and cached derivatives (object storage stand-in)
DATA_DIR=./data
# Comma-separated whitelist of allowed output widths (prevents cache-buster DoS)
ALLOWED_WIDTHS=100,200,400,800,1200
