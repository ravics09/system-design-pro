PORT=3105
# Optional Redis for automatic unique worker-id leasing (0-1023). If unset, WORKER_ID is used.
REDIS_URL=redis://127.0.0.1:6379
# Fixed worker id if not leasing from Redis (must be unique per instance).
WORKER_ID=1
