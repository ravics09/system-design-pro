# Base URL of the NestJS shopping API (server default port is 3021).
# NEXT_PUBLIC_* is inlined at build time; in Docker Compose set this to the
# browser-reachable server URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3021
