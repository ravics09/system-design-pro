# Server runtime
PORT=3015
CORS_ORIGIN=http://localhost:3000
# Token signing + lifetimes (short access, long refresh)
JWT_SECRET=dev-only-change-me
ACCESS_TTL_S=900        # 15 minutes
REFRESH_TTL_S=604800    # 7 days
