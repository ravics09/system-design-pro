# Base URL of the NestJS notifications API + WebSocket server
NEXT_PUBLIC_API_BASE_URL=http://localhost:3004
# Demo authenticated user (stands in for the logged-in user / JWT subject)
NEXT_PUBLIC_USER_ID=alice
