# Server runtime
PORT=3010
CORS_ORIGIN=http://localhost:3000
# Default dataset size seeded on boot
SEED_SIZE=50000
# Guardrail: reject queries returning more than this many rows unbounded
MAX_RESULT=1000
