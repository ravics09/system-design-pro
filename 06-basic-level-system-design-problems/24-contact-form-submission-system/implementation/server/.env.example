# Server runtime
PORT=3014
CORS_ORIGIN=http://localhost:3000
# Rate limiting (per IP, sliding window)
RATE_MAX=5
RATE_WINDOW_MS=60000
# Notification worker (simulated email)
NOTIFY_FAILURE_RATE=0      # 0..1 — inject send failures to see retries + DLQ
NOTIFY_MAX_ATTEMPTS=3
