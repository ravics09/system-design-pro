# Server runtime
PORT=3016
CORS_ORIGIN=http://localhost:3000
# Graceful-shutdown timings
PRESTOP_MS=500          # keep serving after failing readiness (lets the LB de-register)
DRAIN_DEADLINE_MS=10000 # force termination if in-flight requests don't finish in time
