# Server runtime
PORT=3007
CORS_ORIGIN=http://localhost:3000
# Cache bounds
CACHE_MAX=100          # max entries before LRU eviction
CACHE_TTL_MS=60000     # default entry TTL
# Origin (simulated slow DB) latency in ms — makes the miss/hit gap visible
ORIGIN_LATENCY_MS=120
